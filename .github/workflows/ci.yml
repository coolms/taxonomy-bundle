name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
  # Weekly, so a break from a dependency or a PHP point release shows up on a
  # Monday rather than inside someone's pull request.
  schedule:
    - cron: '0 6 * * 1'

permissions:
  contents: read

jobs:
  test:
    name: PHP ${{ matrix.php }} ${{ matrix.deps }}
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        php: ['8.5']
        # --prefer-lowest is not padding. The constraints in composer.json are
        # a promise about what this works with, and the only way that promise
        # gets tested is by installing the floor of every range. Without it a
        # constraint can drift years below what the code actually needs and
        # nobody finds out until a consumer with an older lock installs it.
        deps: ['highest', 'lowest']

    steps:
      - uses: actions/checkout@v4

      - name: Set up PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ matrix.php }}
          coverage: none
          # Everything up to xmlwriter is PHPUnit's, from its composer.json
          # plus what its bootstrap checks at runtime; ctype and tokenizer are
          # runtime-only, so a list built from `require` alone installs fine
          # and then refuses to run. curl and phar are Composer's. pdo_sqlite
          # lets DBAL build a platform without a server, and zip is required by
          # coolms/core-application -- omitting it fails the RESOLVE, not the
          # run, so it reads like a dependency problem rather than a missing
          # extension. field-bundle and core-bundle both list it.
          extensions: none, ctype, dom, filter, json, libxml, mbstring, tokenizer, xml, xmlwriter, curl, phar, pdo, pdo_sqlite, zip

      - name: Validate composer.json
        run: composer validate --strict

      - name: Cache Composer packages
        uses: actions/cache@v4
        with:
          path: ~/.composer/cache
          key: composer-${{ matrix.php }}-${{ matrix.deps }}-${{ hashFiles('composer.json') }}
          restore-keys: composer-${{ matrix.php }}-${{ matrix.deps }}-

      # No composer.lock here (it's a library), so this resolves fresh every
      # run and catches a dependency drifting out from under the constraints.
      #
      # The platform siblings ship in LOCKSTEP with this package, so their
      # published tag and this branch are only ever out of step inside the
      # settling window -- which is exactly where CI lives. Pin them to the
      # generation branch so the resolve sees the code this branch was written
      # against. Never in the published manifest, and after
      # `composer validate --strict` above has already seen the real one.
      #
      # Relaxing stability is NOT enough on its own once the generation has an
      # alpha tag: prefer-stable ranks alpha above dev, so v2.0.0-alphaN starts
      # winning over dev-develop and the hatch closes silently.
      #
      # !! A persistence ADAPTER is added here that the published manifest
      # deliberately does not name. A -bundle requires the VIRTUAL
      # coolms/core-persistence-implementation transitively and leaves the
      # choice of provider to the application -- that is the seam working, and
      # it means every bundle in this estate fails to install ALONE. CI has to
      # play the application and pick one, or it would report a packaging
      # defect that is not there. It stays out of composer.json: putting it
      # there would hard-wire the adapter and destroy swappable persistence.
      #
      # Scoped deliberately. coolms/rql is an INDEPENDENT library on its own
      # major, so its published tag is the right thing to test against.
      - name: Resolve the platform siblings from the generation branch
        run: |
          composer config minimum-stability dev
          composer config prefer-stable true
          # A new coolms dependency has to be classified before it can be
          # resolved, so this list cannot go stale without the build saying so.
          php -r '
            $d = json_decode(file_get_contents("composer.json"), true);
            $known = ["coolms/core", "coolms/core-bundle", "coolms/entity",
                      "coolms/entity-bundle", "coolms/entity-doctrine",
                      "coolms/rql", "coolms/taxonomy", "coolms/taxonomy-doctrine"];
            $seen = array_merge(array_keys($d["require"] ?? []),
                                array_keys($d["require-dev"] ?? []));
            $new = array_diff(array_filter($seen,
                static fn ($p) => str_starts_with($p, "coolms/")), $known);
            if ($new) {
                fwrite(STDERR, "unclassified coolms dependency -- classify it in "
                    . "this workflow before it can be resolved: "
                    . implode(", ", $new) . PHP_EOL);
                exit(1);
            }
          '

      - name: Install dependencies
        run: |
          WITH='--with coolms/core:2.0.x-dev'
          WITH="$WITH --with coolms/core-bundle:2.0.x-dev"
          WITH="$WITH --with coolms/entity:2.0.x-dev"
          WITH="$WITH --with coolms/entity-bundle:2.0.x-dev"
          WITH="$WITH --with coolms/entity-doctrine:2.0.x-dev"
          WITH="$WITH --with coolms/taxonomy:2.0.x-dev"
          WITH="$WITH --with coolms/taxonomy-doctrine:2.0.x-dev"
          # The adapter the APPLICATION would choose -- see the note above.
          composer require --no-update --dev 'coolms/core-doctrine:^2.0'
          if [ "${{ matrix.deps }}" = 'lowest' ]; then
            composer update --prefer-lowest --prefer-dist --no-interaction --no-progress $WITH --with coolms/core-doctrine:2.0.x-dev
          else
            composer update --prefer-dist --no-interaction --no-progress $WITH --with coolms/core-doctrine:2.0.x-dev
          fi

      - name: Lint sources
        run: find src tests -name '*.php' -print0 | xargs -0 -n1 -P4 php -l

      - name: Run tests
        run: vendor/bin/phpunit --testdox


  boundary:
    name: No application code
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # This package was extracted out of an application, and the whole point
      # of the extraction was that it stops depending on one. Asserted rather
      # than trusted, because a single `use App\...` added later would compile
      # fine here and break only for consumers.
      - name: Assert nothing references an application namespace
        run: |
          if grep -rnE '\bApp\\+[A-Z]' src/ ; then
            echo "::error::src/ references an application namespace"
            exit 1
          fi
          echo "confirmed: no application namespace in src/"

      # The API surface is pinned on purpose: explicit shortName, explicit
      # uriTemplate per operation, explicit operation name. A namespace change
      # must not be able to move a path, an operation name, @type or the
      # context IRI. Dropping an explicit shortName would move the last two
      # silently, so its presence is asserted rather than trusted to review.
      - name: Assert the API resources still pin their shortName
        run: |
          missing=0
          for f in src/ApiPlatform/Resource/*Resource.php; do
            grep -q 'shortName:' "$f" || { echo "::error::$f does not pin shortName"; missing=1; }
          done
          [ 0 -eq "$missing" ] || exit 1
          echo "confirmed: every API resource pins its shortName"
